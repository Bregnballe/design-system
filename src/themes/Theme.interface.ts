export interface ThemeProps {
    colors: {
      primary: String,
      secondary: String,
      tertiary: String,
      neutral: String, 
    },
    bodyText: String,
    borderRadius: String,
    borderWidth: String,
    shadowWidth: String
  };
  