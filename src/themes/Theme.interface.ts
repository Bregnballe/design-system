export interface ThemeProps {
    colors: {
      primary: String,
      secondary: String,
      tertiary: String
    },
    bodyText: String,
    borderRadius: String,
    borderWidth: String,
    shadowWidth: String
  };
  